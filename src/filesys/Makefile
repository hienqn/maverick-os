# -*- makefile -*-
# File System project Makefile - delegates to shared kernel build system
# Run "make" here to build kernel with full filesystem (buffer cache, extensible files)
include ../Makefile.kernel

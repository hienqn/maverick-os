{
  "version": 1,
  "source": "tests/vm/mmap-over-data.ck",
  "type": "expected",
  "options": {
    "ignore_exit_codes": true
  },
  "expected": [
    "(mmap-over-data) begin",
    "(mmap-over-data) open \"sample.txt\"",
    "(mmap-over-data) try to mmap over data segment",
    "(mmap-over-data) end"
  ]
}

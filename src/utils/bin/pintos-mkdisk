#!/bin/bash
# Drop-in replacement for the Perl pintos-mkdisk script
# Runs the Bun/TypeScript version

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PATH="$HOME/.bun/bin:$PATH"
exec bun run "$SCRIPT_DIR/../src/mkdisk.ts" "$@"

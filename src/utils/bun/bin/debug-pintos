#!/bin/bash
# Agent-friendly debugger for PintOS
# Runs the Bun/TypeScript debug-pintos tool

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PATH="$HOME/.bun/bin:$PATH"
exec bun run "$SCRIPT_DIR/../src/debug-pintos.ts" "$@"

#!/bin/bash
# PintOS grading script - Bun/TypeScript version

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PATH="$HOME/.bun/bin:$PATH"
exec bun run "$SCRIPT_DIR/../src/make-grade.ts" "$@"
